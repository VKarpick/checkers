#include <iostream>

#include "ai/train_checkers.h"


int main()
{
    //TrainCheckers training(6, 1, true, "weights.dat", "weights.dat");
    /*TrainCheckers training(1, 2, true, "", "");
    training.train();*/

    /*Node<int> node(1);
    Node<int> node2(2);

    node.add_child2(node2);
    std::cout << node.children_nodes().size() << std::endl;

    node.remove_child(node2);
    std::cout << node.children_nodes().size() << std::endl;
    std::cout << (node2.parent() == nullptr) << std::endl;*/


    //TODO replace pointers with references
}
